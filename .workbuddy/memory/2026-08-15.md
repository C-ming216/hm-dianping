# 2026-08-15

## 检查：商店类型查询板块（ShopType）

功能链路：`GET /shop-type/list` → ShopTypeController.queryTypeList() → ShopTypeServiceImpl.quryShopType()（Redis 缓存 → 查库 → 写缓存）→ Result.ok(typeList)

**结论：功能能跑，但发现问题清单：**
1. 🔴 Redis 缓存 `shop:list:type:` **无 TTL**（`set()` 没传过期时间）→ 缓存永久有效，类型数据更新后前端永远看到旧数据（缓存一致性隐患）
2. 🟠 方法名拼写错误：`quryShopType` → 应为 `queryShopType`（接口 IShopTypeService + 实现都错了）
3. 🟠 ShopTypeController 未使用 import：`com.hmdp.service.impl.ShopServiceImpl`
4. 🟡 `@Resource StringRedisTemplate stringRedisTemplate;` 缺 `private`
5. 🟡 空列表也会被永久缓存（放大问题 1）
6. 🟡 提示：ShopType 的 `@JsonIgnore` 是 Jackson 注解，hutool JSONUtil 不识别，存 Redis 的 JSON 里仍含 createTime/updateTime（对前端无影响，最终走 Jackson 序列化）

**修复方案（待用户确认）**：set 加 TTL、改名 queryShopType、删多余 import、加 private

## Git 提交与推送（后端 → GitHub）

- 用户已自行把 `quryShopType` 改为 `queryShopType`（拼写修正），但 Controller 里多余 import `ShopServiceImpl`、缓存无 TTL 等问题**未修就提交了**（用户选择直接提交现状）
- 后端提交：`c03f609 feat: 商店模块加入Redis缓存，登录模块完善`（11 文件）
- 后端推送：origin 原为 HTTPS，无凭据导致 push 失败 → 本机有 `~/.ssh/id_ed25519` 且已绑定 GitHub（ssh -T 验证 Hi C-ming216!）→ 改用 SSH remote `git@github.com:C-ming216/hm-dianping.git` 推送成功
- 前端仓库：`E:/JAVA/Project/web-front-hm-dianping/nginx-1.18.0/html/hmdp`，origin 指向 **gitee 课程仓库** `huyi612/hmdp-web`（非 GitHub），用户选择**只本地提交不推送** → `bc6b714 feat: 登录模块适配JWT双Token，修复验证码倒计时`（4 文件）
- 前端还有未跟踪的测试图片 `imgs/blogs/`（未提交）
- 环境备注：沙箱内 git fetch 无法写入 refs/remotes/origin/（显示 [gone]），不影响实际推送；用户在自机终端 fetch 即可恢复

## 缓存穿透防护落地（方案一 + 方案三）

2026-08-17 用户要求实现缓存穿透防护，落地内容：
- **ShopServiceImpl.queryShopById**：
  - 方案三：`id==null || id<=0` 直接 `Result.fail("店铺id不合法")`，不打缓存不打库
  - 方案一：查库为空 → 缓存 `""` 空标记，TTL=CACHE_NULL_TTL(2分钟)；命中空标记返回 `Result.fail("店铺不存在")`
  - 修掉原隐患：`JSONUtil.toJsonStr(null)` 会写 `"null"` 进缓存且 TTL 30 分钟（原代码隐式缓存空值但语义混乱）
  - 顺手修：`update()` 里 `return null` → `return Result.ok()`（原为 bug，前端收不到成功响应）；字段加 `private`
- **ShopTypeServiceImpl.queryShopType**：空列表 → 缓存 `"[]"` + CACHE_NULL_TTL(2分钟)；有数据 → 正常 TTL(30分钟)；字段加 `private`
- **ShopTypeController**：删除冗余 import `com.hmdp.service.impl.ShopServiceImpl`
- 编译通过（68 源文件）

遗留：布隆过滤器（方案二）未做，数据量大后再考虑。

