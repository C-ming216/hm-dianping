# 2026-08-29

## 协作规则（长期有效）
- 用户明确要求：**未经其明确指示，禁止修改代码**，只给回答和方案（已同步写入用户级 ~/.workbuddy/MEMORY.md 和项目 MEMORY.md）

## 一人一单方案（VoucherOrderServiceImpl）
- 给出分层防护设计：count 预校验（体验层）→ synchronized 按 userId 加锁且**锁包事务**（正确性层）→ DB 唯一索引 `uk_user_voucher(user_id, voucher_id)` + DuplicateKeyException 兜底（底线）
- 详细 6 步开发方案已交付（DDL → 接口加 createVoucherOrder → @EnableAspectJAutoProxy(exposeProxy=true) → 入口改造加锁 → 事务方法实现 → JMeter 并发验证），用户自行编码
- 关键坑已强调：锁必须在事务外、禁止 this 自调用（走 AopContext.currentProxy()）、synchronized 锁对象用 userId.toString().intern() 而非 Long
- 顺带指出的存量问题：seckillVoucher 返回值错（返回了 voucherId 应为订单 id）、save 未判返回值、stock 预查属体验层
- 状态：方案已交付，待用户编码实现
